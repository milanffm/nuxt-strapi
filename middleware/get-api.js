export default async ({store}) => {
  // console.log('MIDDLEWARE API', store.getters['post/getPosts']);
  // if (process.server || store.getters['post/getPosts'].length > 0 ) return; // run only in client side, not while generating static pages...
  // try {
// 	await store.dispatch('post/get');
//
  // } catch (error) {
// 	throw error;
  // }
}